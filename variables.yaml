- name : play level variables
  hosts: localhost
  vars:
    course: "Ansible"
    trainer: "Siva"
    duration: "120 hours"
  tasks:
    - name: display play level variables
      vars:
        course: "Devops with Aws"
      ansible.builtin.debug:
        msg: "Course: {{ course }}, Trainer: {{ trainer }}, Duration: {{ duration }}"